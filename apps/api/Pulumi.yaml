name: rotom
description: Serverless terminal-based TUI for managing budgets
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
config:
  pulumi:tags:
    value:
      pulumi:template: serverless-aws-typescript
